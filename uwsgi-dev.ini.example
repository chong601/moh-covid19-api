# Duplicate this file and remove .example to use this uWSGI config
[uwsgi]
module = wsgi:app
master = true
processes = 2
threads = 100
vacuum = true
socket = 0.0.0.0:5000
protocol = http
threads = true